# Lakelet — build session log, September 11, 2026

*Hants and Claude (Fable 5.1) · The order after session 6, the task list restructured, and the two core items decided and built*

## 1. The order, and the list

Hants deprioritised session 7 (the ask box) and set the order after step 5 of the app brief: the two core items found by using the app (the probe, snapshot expiry), then session 10, 9, 8, 5. `TASKS.md` was restructured around that: a "Now", a "Next, in order", a list of what needs another machine or Hants, and a map of the ten sessions with a status column, so it is the one file to open; the session map (`lakelet-build-sessions.md`) points at it as the only place status lives. The session 6 record became one row per step.

## 2. Four decisions, decided

`decisions-for-review_091126.md`, all four accepted the same day: the probe reads with the cache bypassed in Python rather than through DuckDB; where it cannot, the cached figure is capped at 7,000 MB/s and labelled; `lakelet gauge probe` measures again and a figure from before the change is flagged; expiry is `lakelet tables expire` with a `keep_snapshots_days` key (7), deleting files only when asked, never for an attached table.

## 3. The probe, built

`gauge/inputs.py`: the probe no longer involves DuckDB. `_write_uncached` writes 512 MiB of random bytes (a 4 MiB `urandom` block, repeated, so no controller compresses it) and `_read_uncached` reads them back, both through `os.open` with `O_DIRECT` on Linux (an anonymous `mmap` as the page-aligned buffer, `os.writev` and `os.readv` in 4 MiB chunks) or `fcntl(F_NOCACHE)` on macOS, the read returning seconds and the method. The write has to bypass the cache as well: Hants' first run of `lakelet gauge probe` on the Mac reported 16,592 MB/s with `F_NOCACHE` on the read alone, because macOS serves pages that are already in memory (the write had just put them there) even to a no-cache read; Linux's `O_DIRECT` reads from the disk regardless, which is why the container was honest from the start. A figure above 20,000 MB/s is treated as a bypass that did not bypass and falls to the cached path; when neither works the file is read through the cache twice, the second time kept, capped at the ceiling, method `cached`. `probe_throughput` returns a `ProbeResult(mbps, method, size_bytes)`; `probe_method(cache)` reads the method back from `machine.json` and calls a figure with none recorded `cached`, which is what every project from before today has. `Project.run_probe` is the one place that runs it and writes the cache, used by `init` and by the new `lakelet gauge probe [--mb]`. `init` says "up to N MB/s (measured through the cache…)" when it had to; `gauge history` opens with the disk line and, for a cached figure, the instruction to re-probe; `/api/health` gains `throughput_probe`; the app's tile reads "local disk (cached; run lakelet gauge probe)" with the reason in its tooltip.

Measured in the container with `O_DIRECT` on both sides: 5,090 MB/s for 512 MiB in 1.0 s (the container's disk is a virtual one with a host behind it, so the figure is what the guest can get, not a bare NVMe). The Mac's number is Hants' to see after the write-side fix: the first attempt said 16,592.

Tests: `test_step5_gauge.py` (the method is `direct` on Linux and `nocache` on macOS, the figure under 20,000 when the cache was bypassed, the file removed, `machine.json` carrying `probe` and the size, a re-probe rewriting it, an old cache reading as `cached`); `test_step7_cli.py` (`gauge probe`, `gauge history`'s line); `test_step9_api.py` (`throughput_probe` in health).

## 4. Expiry, built

`config`: `catalog.keep_snapshots_days` (7) in the section, the default file, and `SETTABLE`, so `lakelet config set` and the app's settings panel take it. `tables.py`: `_expirable(md, keep_days)` (older than the retention, not the current snapshot, not a branch or tag target), `_referenced_files(md, only, exclude)` (manifest lists, manifests, data and delete files with their sizes, across the chosen snapshots), `_orphans(location, kept, grace)` (data and manifest files under a local table's own `data/` and `metadata/` that no snapshot references and that are older than the grace period, an hour by default; metadata JSON stays), and `expire(name, keep_days, orphan_grace_seconds)`: refuses an attached table (the `lakelet.source-prefix` property) or one outside the warehouse, loads the table through the project's own REST catalog with pyiceberg, `maintenance.expire_snapshots().older_than(newest expirable + 1 ms).commit()`, re-reads the metadata, deletes what the expired snapshots referenced and the kept ones do not plus the orphans, and reports. The catalog accepted pyiceberg's `remove-snapshots` update without a change: the commit path already applies updates with `update_table_metadata`, which is why it was built that way. `describe` gains `expirable_snapshots`, `reclaimable_bytes` and `keep_days`, and the CLI prints "N snapshot(s) older than D days, X reclaimable: lakelet tables expire <name>". `lakelet tables expire <name> | --all [--keep-days N]`; `POST /api/tables/{name}/expire {keep_days?}` with 409 `not_expirable`.

One thing found on the way: `import --replace` drops and recreates the table, and the previous table's files stay in the same folder, unreferenced by any snapshot of the new one. That is what the orphan sweep is for, and its test covers it: after a replace the old files linger, an expire inside the grace period leaves them, an expire with no grace removes exactly them and the table reads back whole.

Tests, `tests/test_expire.py`, five: a create and three in-place rebuilds (the dbt path, delete then insert) expire down to one snapshot with files removed and every remaining file present, the table reading back through DuckDB and pyiceberg, a further rebuild and expiry; the retention keeping minutes-old snapshots; the replace orphans; an attached table refused with its files untouched; the verb, the setting and `describe`'s line through the CLI. Plus the route and the setting in `test_step9_api.py`.

## 5. Docs and the rest

The tables page has a "Snapshots, history and expiry" section; the gauge page says how the throughput is measured and what `cached` means; the config page has the key; the API page has the route, the describe fields and `throughput_probe`; the CLI reference is regenerated. The app: the retention row in settings, the tile's flag.

Hants ran the probe on the Mac: 16,592 MB/s at first (the read alone bypassed), then 9,949 after the write-side fix, `nocache`; an Apple SSD peaks at 7,000 to 8,000 for reads, so a little of the just-written data is still served from memory, and the figure is within the gauge's 2× target where 84,914 was off by twelve. `describe` on a once-imported table showed one snapshot and no expiry line, as it should.

## 6. Step 5 of the app brief, closing session 6

§6 of `app-v0-plan.md` now carries a number or a test against every line, ticked, except the week of daily use. Two gates were added for "nothing hidden": `step5-nothing-hidden.spec.ts` records every request across screen 1 (a preview and an import), screen 2 (a query with a bar chart) and the settings panel, and allows only the dev server and the sidecar on loopback (56 requests, two hosts); `a_release_build_passes_no_dev_origin` in the supervisor's tests asserts, under `cfg!(debug_assertions)`, that a debug build passes the Vite origin and a release build passes nothing, and is meant to be run with `cargo test --release` (done once in the container, 191 s for the release build; CI keeps the debug build). Yellow joined the gauge-line Vitest. The docs gained `/docs/app` (running from source, projects and windows, both screens, the chart's rule, settings, the keys, how the window talks to the core, nothing hidden, the tests, what to do when it does not start), the overview's status table names the app, and `lakelet-session-6-desktop-shell.md` is the session's record for whoever opens session 10.

## 7. The session 10 brief

`ship-v0-plan.md`, in the shape of the app brief: S1 to S14 for Hants to tick, then a build order of six steps with gates. The recommendations, in one line each: PyInstaller one-directory as a Tauri resource, not `externalBin` and not one-file (a one-file build unpacks 250 MB on every launch); the four extensions downloaded at build time and laid beside the frozen core, the core reading `LAKELET_EXTENSION_DIR`, so `audit network` from the bundle reads zero including `init`; the size measured and the docs saying the number, with a trimming order (pyarrow's Flight and Substrait first) if the download is over 200 MB; an Apple-silicon DMG signed and notarised from CI with an API key, Intel only if a runner offers it; a `.deb` leading the AppImage on Ubuntu, because the `.deb` is the one that needs no terminal; GitHub Releases as the source, a tap with a cask, and the honest amendment that the CLI's `brew` path is `pipx` or `uv tool` from PyPI (a Homebrew formula would build pyarrow from source); PyPI by trusted publishing with the version checked against the tag and `tauri.conf.json`; auto-update deferred; the app offering to link its own `lakelet` into `~/.local/bin`; the correction as a bounded median ratio per machine and per dominant operator class after twenty runs, applied to local wall time only, with the TPC-H harness as its gate; `lakelet gauge export` as the F0.3.9 file sent by hand and the sharing toggle left off until there is a receiver (a deviation from the PRD, recorded); the engines smoke through `catalog serve` on the Mac; `/docs/partners`; Windows built and allowed to fail. Three items need Hants alone (the Apple account, the PyPI name, a clean Mac) and are in `TASKS.md`.

## 8. Not yet: the real-data round first

Hants accepted S1 to S14 and held the build: too early to ship before the product has met a real bucket, dbt views work, and the app has more than two screens. What was already mapped: the step 8 suite takes a real store through `LAKELET_TEST_S3_ENDPOINT` and had only met Moto and RustFS; the demo bucket was a Hants item; session 9 held dbt with the materialisation shipped early; screens 3 to 10 were spread over sessions 5, 7, 8 and 9; nothing owned views, attach from the app, a table's detail or the questions the CLI already has. He chose: dbt `view` models and dbt in the app; his own AWS bucket; attach from the app, the Gauge screen and the table detail. `real-data-plan.md` is the brief: R1 to R10 (the order; the suite against his bucket with a scoped IAM user; a public-read demo bucket with a public-domain dataset above 15 GB so it is Red from a laptop; attach with discovery first and the CLI's credential chain; `lakelet run` with the DAG by verdict and dbt as a `lakelet[dbt]` extra; views as Iceberg views in Lakelet's own catalog with the engine creating DuckDB views of them on connect; the table detail; the Gauge screen with `gauge export` pulled forward from the ship brief; the first Arrow flush; the gates) and eight steps. Git auto-commit and screen 9 go to the round after.

## 9. R1 to R10 decided; step 0 of the real-data round

Hants ticked all ten and answered R3 with a better idea: a dataset AWS already hosts publicly. The answer is in the brief: read-only is enough because D26 keeps an attached table's metadata on the laptop, so the demo becomes a Registry of Open Data dataset, no bucket of ours, and step 1 gains anonymous access (`--anonymous`; refused today) and the choice of dataset.

Step 0 built: `tests/s3_helpers.py` is the one object store for the S3 tests (`test_step1_s3.py`, `test_step8_remote.py`), Moto by default, a real bucket when `LAKELET_TEST_S3_BUCKET` names one (never created, `head_bucket` checked before anything is written, every key under `lakelet-tests/<date>-<id>/`, that prefix and `_lakelet/` deleted at module end and at the next start), a self-hosted store when only the endpoint is set (the old RustFS path, `lakelet-test` created if missing); the endpoint is optional so AWS itself needs only the keys and the region; the DuckDB writer's secret and pyiceberg's properties come from the store so neither test hardcodes an endpoint or a region any more. `/api/health` gains `aws: {configured, source, profile, region, endpoint}` from `S3Settings.describe()`, never a key, with the test asserting the secret is not in the response. `/docs/remote` on the site: credentials, the read-only policy for a partner's bucket, the suite's policy and variables, what it writes and removes, the cost. Against Moto: 21 passed (the disconnect test in `test_step9_api.py` failed once under load in the container and passed alone; unrelated). The real run is Hants', from the Mac, once the bucket and the IAM user exist; the numbers go here.

## 10. The real run, and what it found

Hants ran both S3 files from the Mac against a private bucket in `us-east-1` with a scoped IAM user: 9 passed, 1 failed, 1 skipped, 3 min 20 s in all (the fixtures cross the internet three files at a time). The catalog with its warehouse in the bucket, attach in place with no object copied, metadata in the bucket and register by metadata location, refresh adding a file and refusing a missing one, drift and hive refused by name, discover, the CLI: all as against Moto. The failure was the budget: the second estimate on a bucket-metadata table took 655 ms against the 150 ms the core brief promises, with the manifests cached on disk as the test asserts. The cause was in plain sight once looked for: `ManifestCache` cached the Avro but read the table's `metadata.json` through the uncached `MetadataIO` on every estimate, `_table_scans` read it a second time for the column list, and the catalog server behind DuckDB's `EXPLAIN` read it a third time for `load_table`; three GETs to Virginia, each a round trip, before DuckDB's own reads of the manifest list and manifest. Moto on loopback had hidden all of it.

Fixed by moving the caching FileIO from the gauge into `catalog/commit.py`: `MetadataIO(properties, cache_dir)` wraps its FileIO so every remote `.metadata.json` and `.avro` is kept under `.lakelet/cache/objects/` after the first read (Iceberg never rewrites a file under the same name), and the project hands the cache dir to its own `MetadataIO` and to the catalog server it embeds; the gauge's `ManifestCache` uses that same IO. A local `file://` warehouse is never cached. The gate grew: the bandwidth test now spies on the inner FileIO and asserts the second estimate opens nothing remote from Python; without the fix it lists the three reads. What is left on the network for a second estimate is DuckDB's own binding of the table for `EXPLAIN` (the manifest list and the manifests, through httpfs, with a HEAD per cached file under `validate_external_file_cache`'s default), which Python cannot cache for it. `tests/smoke/remote_timing.py` is the instrument: it times each phase of three estimates against the store the environment names and prints the table; Hants runs it on the Mac and the numbers decide whether DuckDB's reads need a setting (`enable_http_metadata_cache`, which the script also tries) or the budget is met as is. The full suite: 157 passed, 9 skipped in the container.

After the cache, on the Mac against the bucket in `us-east-1`: the two S3 files 10 passed, 1 skipped in 21 s (from 3 min 20 s), the second estimate on a bucket-metadata table 3 ms (from 655). The instrument's table: attach with the metadata in the bucket 2.9 s (the metadata writes, the manifest, and the bandwidth probe's read); the first `EXPLAIN` through DuckDB 164 ms (its REST `load_table`, answered from the server's cache, and its own reads of the manifest list and manifest from S3), the second and every later one 1 ms, so DuckDB keeps its binding for the connection and needs no setting; the metadata and manifests through Python 0 to 1 ms; the whole estimate 8 ms the first time, 3 ms after. The 150 ms budget for the second estimate holds on a real link with room to spare; the first estimate on a never-read remote table is DuckDB's 164 ms plus the probe, which the PRD's 800 ms allows. Step 0 of the real-data round is done.

## 11. Step 1, the core half: a public bucket needs nothing

`--anonymous` on `tables discover` and `tables attach` (and `anonymous` on the two API calls): the listing and the footer reads go through a pyarrow filesystem with `anonymous=True` and no region assumed (pyarrow asks S3 for the bucket's region), pyiceberg's client for the table carries `s3.anonymous=true` so `add_files` and `refresh` read the same way, the table carries `lakelet.anonymous = true`, and the engine gets a DuckDB secret scoped to that bucket alone with no key (`PROVIDER config`, `SCOPE 's3://<bucket>'`, the region resolved), which wins over the project's default secret by the longer scope and sends the bucket's requests unsigned. The bucket and its region go into `.lakelet/public-buckets.json`, read at every `Project.open` so the secret exists before the first query. `--anonymous` with `--metadata-in-bucket` is refused (nothing to write with); `--anonymous` on a metadata location is refused too. `TableInfo` gains `source` and `public` for the panel in step 2. The test: Moto enforces ACLs as S3 does, so the fixture grants public-read on the bucket and the objects, the keys are removed from the environment, `discover` without the flag is refused with the credentials sentence, with the flag it lists; attach lands with the metadata local, DuckDB counts 3,000 rows through the scoped secret (an unsigned request Moto's ACL check allows, a signed empty one it would not), `refresh` adds a file, the estimate says remote, and a re-opened project reads the table and lists it as public. Skipped against a real bucket (a test does not make Hants' bucket public). Docs: the tables page's "Public buckets" paragraph, the remote page's section, the API page, the CLI reference regenerated. Full suite 158 passed.

The dataset is chosen on the Mac (the container cannot reach S3): candidates and the commands are in `TASKS.md` under "needs Hants"; the result and the reason go here.

## 12. The dataset: Overture Maps

The first try from the Mac was a 301: the Overture bucket is in `us-west-2`, pyarrow given no region assumes `us-east-1` and does not follow S3's redirect (it resolves a bucket's region only in `from_uri`). Fixed: for an anonymous bucket the core asks S3 for the region once per bucket per process (`pyarrow.fs.resolve_s3_region`, an unsigned HEAD) and hands it to the listing, pyiceberg's client and the DuckDB secret. Then, with no AWS keys in the shell: `discover` on `release/` listed two releases of about 611 GB each, on `release/2026-08-19.0/` six themes (addresses 21.9 GB in 32 files, base 199.7, buildings 277.4, divisions 5.6, places 10.5 in 16 files, transportation 96.5). `attach addresses --anonymous` registered 472,797,160 rows in place, `places` 73,631,092, the metadata local, both reading without credentials. The bandwidth probe said 111 Mbps for the link at the time (the site's reference is 190). The gauge over the manifests it wrote: `count(*)` on the addresses scans 0 B; `group by country` scans 218.9 MB, about 16 s; `avg(confidence)` on the places 211.3 MB; a bounding-box count over the places (Stony Brook's box on `bbox.xmin`, `bbox.ymin`) scans 104.8 MB, estimated 8 s, ran in 4.08 s and found 9,455. The nested `bbox` struct pruned. The dataset is chosen: Overture's `addresses` (over the 15 GB line, few files, one schema) with `places` beside it; `/docs/remote` has the commands and the numbers, and says to pick a current release from `discover`. One fix on the way: `tables list`'s `where` column said `local` for both (it showed where the metadata was); it now says `public s3://…` or `attached s3://…` with the prefix, `local` for a table Lakelet wrote.

pyiceberg from another process, on the Mac, over `places` with `s3.anonymous` and the region: 17 columns, 16 of 16 files planned for a `bbox.xmin` range (the files are not sorted by longitude, so file-level pruning cannot help; row groups did), 126,285 rows read anonymously. Step 1 is done; the Red estimate over `select *` is a one-liner for whenever.

## 14. Step 2: attach from the app

Core: `Tables.preview_remote(prefix, anonymous)` through `register.inspect`, the shape of a file's preview plus `remote`, `files`, `bytes`, `anonymous`, the columns from one footer with the Iceberg type pyiceberg would give each (the drift check across every footer stays `attach`'s); `POST /api/preview` takes an `s3://` path and `anonymous` and answers with it, 400 `not_registrable` with the credentials sentence when there are none; `register.remote_name` names a table from the prefix's last segment (`type=place` gives `place`). App: the drop zone's box takes an `s3://` prefix, shows the public-bucket switch and, when health says the core has no credentials, the line naming the variables; its command becomes `lakelet tables discover [--anonymous] <prefix>`. The preview panel in remote mode: the files and bytes, "read in place, nothing is copied", the columns under an Arrow heading, "Attach as <name>" with `lakelet tables attach <name> [--anonymous] <prefix>`; a name that exists is refused in place. The tables panel gains a Where column (`local`, or `attached`/`public` with the bucket and the full prefix as its title) and Refresh on an attached table (`lakelet tables refresh <name>`), the notice saying what was added. Tests: Vitest on the command lines, the drop zone (the switch, the credentials line, the discover line) and the remote preview (34 in all); Playwright `attach.spec.ts` against a fifth sidecar whose bucket is a Moto server started by the setup (`tests/moto_fixture.py`, public-read ACLs, a fourth file written when a flag file appears): preview, attach, the Where column, refresh to 4,000 rows, the duplicate refused, and the public switch attaching without credentials; 19 Playwright in all. `/docs/app` says how. Not yet on the Mac: the Overture prefix through the app is the thing to try (`s3://overturemaps-us-west-2/release/2026-08-19.0/theme=places/type=place/`, public ticked).

## 15. Still open

1. Step 2 on the Mac: the Overture prefix through the app.
2. Step 3: the table detail and the first flush.
2. Everything in `TASKS.md` that needs another machine.
