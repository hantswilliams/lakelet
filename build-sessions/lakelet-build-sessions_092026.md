# Lakelet — build session log, September 20, 2026: the two decisions left from the W/L batch

*Hants and Claude. After the website branch merged (`8e9faca`) and the polish items of the 18th, the two open decisions from `lakelet-build-sessions_091826.md` §8, both taken by Hants today: the welcome screen makes a bucket-warehouse project, and history keeps every run. Ship (`ship-v0-plan.md`) is still held.*

## 1. The welcome screen's warehouse field (`src-tauri/src/projects.rs`, `lib.rs`, `Welcome.tsx`)

The container can compile the shell now — the GTK 3 and WebKitGTK 4.1 development packages were the missing piece — so the Rust side was built and tested here rather than left for the Mac. `prepare(executable, folder, warehouse)` passes `--warehouse <s3://…>` to `lakelet init` for a folder that is not a project yet, and refuses a warehouse for a folder that is one already ("its warehouse was fixed when it was set up"), rather than opening the project with a warehouse other than the one asked for. The `open_project` command takes an optional `warehouse` (trimmed; empty is none) through `open_folder` and `start_in_window`; the shell's own opens (the first window, a restart) pass none. The fake sidecar the shell's tests use takes `--warehouse` the way the core does: it refuses anything but `s3://` and writes the value into `lakelet.toml`.

The welcome screen gained **Advanced** beside the button: **Keep the tables in a bucket**, an `s3://bucket/prefix` that goes with the next **Open a folder…** (the recent projects are projects already and never carry it), with the terminal line beside it — `lakelet init <folder> --warehouse s3://…` — and a note that the credentials come from the environment the app was started in. Anything that is not an `s3://bucket/prefix` disables the button and says why, so the core's refusal is never the first thing seen; the core's own error (a project already, no credentials) arrives in the window as any open error does. `openProject(path, warehouse?)` in `lib/session.ts`; `App.open(path?, warehouse?)`. Hants then asked where the box was: the welcome screen is only seen by a window with no project, which a machine with a recent project never shows — so **Open…** in the bar gained **New folder, tables in a bucket…**, a prefix row that then opens the dialog (`OpenMenu.test.tsx`), and the docs name both ways.

Gates: `cargo test` — `a_warehouse_goes_to_init_and_is_refused_for_a_project_that_exists`: the value reaches `lakelet.toml` and the init output, a second `prepare` with a warehouse on the same folder is refused with the sentence, a non-bucket value is refused by the core and no project is made; 9 Rust tests pass. `Welcome.test.tsx`: the box opens, the line follows the value, a bad value disables the button, a good one reaches `onPick` trimmed, an empty one passes nothing. `/docs/app` and `/docs/remote` say the box exists.

## 2. History keeps every run (`history.py` schema 2, the first migration)

`model_runs` and `question_runs` held one row per model and per question — the latest run — so the Changes feed (L2) showed each model's last run beside every one of its snapshots. Hants decided every run should show. Both tables are keyed by the run now (`run_id` the primary key, the model's unique id or the question's slug an indexed column), `record_model_run` and `record_question_run` insert without deleting, `model_last_run` and `question_last_run` take the newest row, and `model_and_question_runs()` (was `last_runs()`) returns every row for the feed. `SCHEMA_VERSION` is 2 and `MIGRATIONS` has its first entry, `_keep_every_run`: each table renamed aside, created in its new shape, its rows copied, the old one dropped — a schema-1 `history.db` is rewritten on open with the last runs it held carried over, through the T4 mechanism that had waited for its first use. `gauge reset` still clears everything.

Gates: `test_changes` — after a second `lakelet run`, `by_c` has two run entries and the question its first, with the name filter agreeing; and a new test builds a schema-1 `history.db` by hand (the old tables, one row each, the version set to 1), opens it, and finds the version at 2, the rows kept, the last runs right, and runs accumulating from then on. `test_recovery`'s "current schema" test now compares each store to its own version (history is 2, the catalog 1); `test_step4_query`'s bookkeeping tests pass unchanged. `/docs/tables`, `/docs/api`, `/docs/recovery` (the sentence saying there were no migrations yet), `status.ts` and the README block, and the Changes screen's legend now say every run.

## 3. Gates

Core **259** passed, 10 skipped; Vitest **99**; Playwright **31**; `cargo test` **9**; `ruff`, `tsc` clean; the CLI reference and README regenerated; the site builds. One container note: the e2e suite filled the disk once (three hundred leftover temp projects plus the new Rust target directory); cleared, and passing.

## 4. `decisions-for-review_092026.md`: Hants' ideas, decided

`hants_ideas.md` (repository root) became six items: P1 a New project flow, P2 the hosting model, U1 the query workspace, U2 the sidebar, U3 one palette, U4 the icon. Hants agreed P1 and U1 to U4 and set P2 aside for more thought. The order is U4, U3, U2 with U1, then P1.

## 5. U4: the app icon (`app/src-tauri/icons/`, `tauri.linux.conf.json`, the site favicon)

Two masters under `icons/src/`, drawn from the site's wave-and-fish mark (`Wordmark.astro`): lime `#d6ee83` on the deep green `#164f44`. `lakelet-mac.svg` puts the tile on the Big Sur canvas (824 of 1024, Apple's corner radius) so it sits level with other Dock icons; `lakelet-linux.svg` fills the canvas, because the desktop neither rounds nor insets. The mark takes 72% of the tile on macOS and 66% on Linux with the wave strokes thickened from 2.5 to 3 so they survive 32 px; at 16 px it is a shape rather than a fish, which the decision allowed. `cairosvg` rasterises each master to 1024 and `npm run tauri icon` makes every size; the sets are pruned to the six files the config names (`icons/README.md` has the three commands). `tauri.conf.json` keeps pointing at `icons/`; a new `tauri.linux.conf.json` — Tauri merges a platform file over the main one — points the Linux bundle at `icons/linux/`. `cargo check` accepts both. The site's `favicon.svg` is the Linux master, so the tab and the Dock agree. Looked at on white and on dark at 256, 128, 32 and 16 before it went in; the Dock itself is Hants' to check on the Mac (`npm run tauri build`, or `tauri dev`'s window icon).

One transport note: the delivery to the Mac stamps every PNG and SVG with a content-credentials block (a `caBX` chunk in the PNGs, a `<metadata>` element in the SVGs — the site's old favicon carried the same), so those files hash differently on the two sides while `icon.icns`, `icon.ico` and the text files match exactly. The block is inert for Tauri, macOS and browsers; the three commands in `icons/README.md`, run on the Mac, regenerate a clean set from the masters if wanted.

## 6. U3: one palette (`web/src/styles/palette.css`)

**What was there.** The app carried a copy of the site's `tokens.css` (A2's "change it there and copy it here") plus its own `theme.css` for dark mode; the site, since the website branch, no longer imported `tokens.css` at all — its live palette was a `.story` block in `exploration.css` layered under a `.website` block in `website.css`, both hard-coded, with Green set to the same lime as the accent. Two palettes, three files, already drifted.

**Now.** `web/src/styles/palette.css` is the one place a colour is defined: a light set and a dark set (the media query when nothing chose light, `data-theme="dark"` when something did — the app's switch writes it, and the site's `Base.astro` pins it), the brand as the green family (deep green `#164F44` on light, lime `#D6EE83` on dark, with `--on-lake` for text on it and `--lake-hover`), the three verdicts the same hues in both sets (the app's dark values; the site's Green is no longer the accent's lime), `--paper` and `--accent` as the site's names for `--bg` and `--lake`, and `--panel-2`, `--ink-soft`, `--head`, `--refused` for the shades the two had each invented. The app imports it from `main.tsx` (Vite's `fs.allow` reaches across to `web/`), its copy and `theme.css` are gone, and a new `base.css` holds the reset it had taken from the copy; `button.primary` reads `--on-lake`; the chart reads the verdict colours and the panel from the document too rather than carrying the light hexes, so the scatter's points are right on dark; the bar's wordmark carries the site's mark. The site imports it first in both layouts, `website.css` and `Waitlist.astro` and `Docs.astro` read tokens where they had hexes, and the `.story` block is `color-scheme: dark` — its palette *is* the dark set. The studio pages' `:root` stopped defining palette variables of their own, which mattered: with `--paper` redefined there, the pinned-dark site rendered light with lime text on the first build, and the aliases now travel inside each set so a later `:root` rule cannot split them from it.

**The editor.** CodeMirror's `defaultHighlightStyle` is a light style, so on a dark window keywords were purple on charcoal. `SqlEditor` now has its own `HighlightStyle` on the tokens — keywords in the brand colour, strings Green, numbers amber, comments muted — and the active line, selection, cursor and completion tooltip read tokens too (`@lezer/highlight` becomes a direct dependency for the tags).

**The name.** The CLI, API and app keep **gauge**; `status.ts`'s label is "Lakelet Lookahead (the gauge)" so the README block and the site say both; `/docs/gauge` already opened with the sentence.

**Gates.** `palette.test.tsx`: both sets present with the named tokens; the app and `Base.astro` import the file; the copy and `theme.css` are gone; every hex in `app/src` and in the site's live stylesheet, layouts and shared components is one the palette defines (the exploration concept pages and the site's illustration components are artwork and are named as outside it); the `.story` block holds no hex. `theme.test.tsx` and `step4-chart-settings.spec.ts` updated to the new values; `versions.spec.ts` takes the first of the run entries now that history keeps every run. Screenshots of the four verdicts on light and dark, the editor, and the site's home went to Hants before this landed; he said to continue. Vitest **102**, Playwright **31**, the site's 66 checks and its build.

## 7. U2 and U1: the sidebar with the explorer, and the query workspace (`app/src/components/Sidebar.tsx`, `Explorer.tsx`, `Split.tsx`, `lib/useTables.ts`, `screens/Tables.tsx`, `screens/Query.tsx`, `App.tsx`)

**What was there.** Five tabs in the bar; the Tables screen was five health tiles, then the SQL box, the gauge line, the chart and the grid, then the tables panel, the drop zone or the preview or a detail — the rows below the fold, and a table's detail a screen's height under the query that reads it.

**The window now.** `App.tsx` lays out the bar, then a `.body` of sidebar and screen, then the status strip. The sidebar (`Sidebar.tsx`) has the five screens as entries with 16px line icons, in the mode's words (`screenLabel`), `⌘/Ctrl+1…5` as their keys (`screenForKey`, in App's key handler beside `⌘,` and `⌘K`; the hint is CSS text from `data-key`, so a button's text is still its name), and **Collapse** to icons, remembered as `lakelet.sidebar`. Under the entries is the explorer (`Explorer.tsx`, the tables panel's successor): the drop zone with **Import…** (the button that was "Choose files…"; the long formats sentence became the input's hover text and a short line), then every table as an entry — name, rows, Where (the same `where-<name>` spans and titles), and a freshness dot whose colour is the age of the last write (green today, amber this week, grey older, none when never written; the time on hover), with the small **Refresh** on attached tables and the open detail's entry marked. The explorer is on every screen, so a click on a table from Models or Lineage lands on the Tables screen with the detail open. The health tiles are gone: the strip (`footer.strip`, still `data-testid="health"`) says the same five things in one line, and the Gauge screen already had the memory limit and the disk figure on its machine line, so nothing was added there.

**The tables' state moved up.** The verbs and state that were the Tables screen's — preview, import, attach, refresh, reattach, relocate, run-stale, publish, open, sample, expire, and the drop listener — are `lib/useTables.ts`, a hook the window calls, because the explorer starts them from any screen and the Tables screen shows what they produce. It tolerates no session (every verb is a no-op until the sidecar is up), and `onShow` brings the Tables screen up when a preview or a detail is about to appear. One consequence a spec caught: a detail opened from a lineage link on the Models screen is still there when the Tables screen comes back (before, the screen remounted empty); `models.spec.ts` waits for the view's heading before reading its kind.

**The workspace.** `screens/Tables.tsx` is the Tables screen: the moved banner when the project was relocated, then the `Query` screen, which now renders `Split` (`components/Split.tsx`) — the editor, the Run bar and the gauge line in the top pane, the chart and the grid in the bottom — with a `panel` slot that takes the results pane when a detail or a preview is open and a `notices` slot for the import report and a drop's error above them; **Run** clears the panel, because the rows are what it is for. The split is a pointer drag on the separator or ↑/↓ with it focused, clamped to 15–85%, the default 36%, remembered as `lakelet.split` (per app rather than per window, since Tauri's windows share the origin's storage — as the mode and theme already are). The editor fills its pane; the grid takes what the chart leaves in the bottom one. With no table yet the top pane says so and the bottom pane still takes a preview, so the first import happens in the same place every later one does; the SQL box's chunk (Arrow, CodeMirror) still loads only with the first table.

**Gates.** `Sidebar.test.tsx` (the entries in both vocabularies, the pressed one, the keys, the collapse and its memory), `Explorer.test.tsx` (rows, Where, the dot's three ages and none, Refresh on attached tables only, the open entry, the empty state and Import…), `Split.test.tsx` (default and remembered share, the arrow keys, a drag from pointer positions, the floor). The `screen-*` test ids moved with the buttons into the sidebar, so no spec changed a navigation click and the "one helper" the decision asked for was not needed; the specs that changed are `step0-health` (the entry is a list item, not a row), `step2-import` (rows and Where on the entry, the size on the detail, `li` count), `gauge` (the explorer stays on the Gauge screen; the workspace is what leaves) and `models` (above). Vitest **111**, Playwright **31**, the app's `tsc` and build, the site's 66 checks and its build. `/docs/app` has a **The window** section and the Tables screen rewritten as the workspace, the keys table has `⌘1…5` and the split's arrows; `remote.md`, `app/README.md` and `status.ts`'s app entry say explorer for panel; the README screenshot is retaken at 2× from the browser build (revenue by month on generated sales data) with the sidebar in it. Screenshots (light and dark workspace, a detail in the results pane, the sidebar collapsed on Models) went to Hants.

## 8. P1: New project… with the bucket check (`core/lakelet/remote.py`, `cli` `bucket check`, `/api/bucket/check`; `app/src-tauri/src/projects.rs`, `lib.rs`; `app/src/components/NewProject.tsx`)

**What was there.** No "new project" as such: the welcome screen's **Open a folder…** with an **Advanced** box for a bucket prefix, and **Open… → New folder, tables in a bucket…** in the bar; both ran `lakelet init` on a folder from the dialog, and the first thing to test the credentials was the first import.

**The check, in the core.** `remote.check_prefix(prefix)` tries the prefix the way a project would use it: the credentials the environment offers (`S3Settings.describe`, the same words health gives), a list of the prefix, then one object `.lakelet-check-<hex>` written under it and deleted. It returns `BucketCheck` — `read`, `write`, `error` in the store's words with the SDK's framing and the request id stripped, `ok`, and a sentence ("… is writable with keys from the environment; one object was written and removed", "…: ACCESS_DENIED during PutObject operation (no credentials in the environment)"). Nothing is created; a prefix that did not exist keeps the zero-byte folder marker pyarrow leaves after the delete (the S3 console leaves the same when a folder is made; boto3 is a dev dependency only, so the marker is not removed). `lakelet bucket check <prefix> [--json]` is the verb, exit 1 when the prefix cannot be written, no project needed; `POST /api/bucket/check {prefix}` is the route for a window that has a core.

**The shell.** `projects::check_bucket` runs `lakelet bucket check --json` on the sidecar executable and carries its JSON as `BucketCheck` (the welcome screen has no core to ask); `projects::new_folder(parent, name)` makes `parent/name`, or takes an empty folder of that name, and refuses a name that is not one segment, a folder that is a project already or one that is not empty; `default_parent` is the Documents folder, else home; `new_project(parent, name, warehouse)` is the folder then `open_folder`, which runs `init` (with `--warehouse`) in this window or a new one. The fake sidecar answers `bucket check` by the prefix's name (`denied-…` refuses the write, `nokeys-…` has no credentials), so `cargo test` covers the shell's reading of the verdict; the real check against Moto is the core's `test_bucket_check.py`.

**The dialog.** `NewProject.tsx`, reached from **New project…** on the welcome screen (the Advanced box is gone; the screen's heading is "A lakehouse is a folder"), from **Open… → New project…** (the bucket row is gone) and by ⌘/Ctrl+N: a name, the parent (the default one, **Choose…** for another; the folder it makes is shown), and **In this folder** or **In a bucket you own** with the prefix field, **Check the bucket**, the no-credentials line when health says the core has none, the result sentence in green or amber, and the `lakelet bucket check` line; the button is **Check and create** until the prefix has passed, and a failed check stops before the folder is made. The line beside it is the `lakelet init … [--warehouse …]` it runs. A browser fills the form and checks a bucket through the core, and cannot make the folder. The check goes through this window's core when it has one (the route) and through the shell otherwise. `openProject`'s `warehouse` argument stays in `session.ts` and the Rust command, unused by the UI now — it is tested and harmless.

**Gates.** `test_bucket_check.py` (4: a writable prefix passes and leaves only the marker, a missing bucket and bad prefixes say why, no credentials is in the sentence, the CLI's exit codes and JSON and the route); cargo `the_bucket_check_carries_the_core_s_verdict_and_words` and `a_new_folder_is_made_under_the_parent_and_never_takes_one_that_is_in_use`; `NewProject.test.tsx` (6), `Welcome.test.tsx` and `OpenMenu.test.tsx` rewritten for the button; `bucket.spec.ts` gains the dialog on the tenth sidecar — a real write under `s3://lakelet-test/new-acme` on Moto passes, a bucket that is not there fails with the store's reason, Create stays off in a browser. Core **263** passed (9 skipped), cargo **11**, Vitest **116**, Playwright **32**, the site's 66 and its build. `/docs/app` has the dialog under Projects and windows and ⌘N in the keys; `/docs/remote` and `/docs/api` name the check; the CLI reference is regenerated. The welcome screen and the dialog were screenshotted for Hants.

## 9. The Mac run (2026-09-21): three races the parallel suite found

Hants' `npm test` and `npm run e2e` on the Mac turned up what the container, with one Playwright worker on two CPUs, never sees. Each is a small change; each a real one.

- **`localStorage.clear` is not a function** in `Sidebar.test.tsx` and `Split.test.tsx`: Node 22+ exposes a bare `localStorage` global that shadows jsdom's (the theme test met the same in September). Both tests now stub their own storage the way `theme.test.tsx` does.
- **`save-question.spec` and `versions.spec` share the eighth sidecar** and run in parallel on the Mac; the versions spec restores the seeded question, so the Review section on the save-question spec's Models screen has an entry that is not its own, and the newest run in the Changes feed may be the other spec's. Three assertions narrowed to *this question left the review* and *the newest run entry about this name* (`filter({ hasText: name })`). The pair passes three of three runs at two workers here.
- **The SQL editor stole focus from the explorer's path box** (`step2-import`, under load): the editor's chunk loads lazily and called `focus()` on mount, so a path being typed landed in the editor. `SqlEditor` now takes focus only when nothing has it. In the same family, **the settings panel's late `settings()` reply reset a value already typed** (StrictMode's second effect run in dev made it visible); the reply now seeds the drafts under what was typed rather than over it.

Vitest **116**, Playwright **32** at two workers (twice), tsc clean. Delivered to the Mac.

## 10. Two things Hants saw in the window (2026-09-21)

**Settings was cut off** at "Record a version" with the keys unreadable: the Tables screen's workspace owns the window's height (`main.workspace-host` is `overflow: hidden`) and the panel rendered above it in the same column. It is a dialog over the window now, as New project… is — `.modal` scrolls, a click on the backdrop or Esc closes it — for the running core and for the "core is not running" line alike; the panel's test ids are unchanged. `/docs/app` says dialog.

**The sidebar's width**: its right edge is a drag handle (`sidebar-edge`, `role=separator`), the pointer's x against the sidebar's left is the width, clamped 200–520, ← and → move it 16px, remembered as `lakelet.sidebar-width`; collapsed keeps its own 52px and the width comes back with the explorer. `Sidebar.test.tsx` gains the drag, the arrows, the bounds and the memory. Vitest **117**, Playwright **32** at two workers.

## 11. Q1: See the answer (2026-09-21; `decisions-for-review_092126.md`, agreed the same day)

**The button.** Every question card in Simple mode has **See the answer** first in its footer (Technical: **Rows** on the model detail's footer): `onAnswer(name)` in `Models.tsx`, which App turns into `arrive = "select * from <name>"` and the Tables screen. `Query` takes `arrive` and runs it on mount as if typed and Run pressed, then tells the window through `onArrived` so a re-render does not run it twice; the box holds the SQL, editable, with `lakelet sql '…'` beside it. A question never refreshed has no rows: the button says **Refresh, then see the answer** (Technical: **Run, then rows**), runs it, re-plans that one model and goes to the rows only if it built. `words` gains `answer` and `answerAfterRun`. The card's wait sentence no longer says "about under a second": a sub-second estimate is "Ready right away."

**What it found.** Under StrictMode (development) the arrival effect runs twice: the first run is aborted by the simulated unmount, the second runs. Two bugs came out of that, both real outside StrictMode too. In the app, a run that a newer one had replaced still wrote its late "stopped" (or rows, or verdict) over the newer run's state; every state write in `Query.run` is now behind "this is still the current run" — Run pressed twice quickly had the same shape. In the core, a client gone *before the first byte* (the fetch aborted the instant it was sent) cancelled the response before the Arrow stream's generator ever ran, so its `finally` — which released the engine lock — never ran, and every later query waited on the lock for good; reproduced with a raw socket outside the app. The lock is released outside the generator now (`_stream_until_gone` takes a `release`, called once on the normal end or by `_stop` after the abandoned thread is closed), pinned by `test_a_client_gone_before_the_first_byte_frees_the_lock`. Esc mid-stream was already handled and still is.

**Gates.** `Models.test.tsx` (both modes' words, the hand-off, the never-refreshed path), `save-question.spec` (never built says so; after the run, Rows shows the verdict, the grid with the question's rows and the line), `models.spec` accepts "Ready right away". Core **264** passed / 9 skipped; Vitest **118**; Playwright **32** at two workers; ruff and tsc clean. `/docs/app`'s Simple paragraph has the button.

## 12. Overture's geometry column refused every read (2026-09-21; `core/lakelet/engine.py`, `dbt/plugin.py`)

Hants: `select * from place limit 10` on the demo project failed with "failed to cast column geometry from GEOMETRY('OGC:CRS84') to BLOB". Two probes on the Mac (a temp project, the first Overture file) found it. The file is not a native Parquet GEOMETRY type: it is `BYTE_ARRAY` with a GeoParquet `geo` footer entry, which attach registers as `binary` (right). DuckDB's `enable_geoparquet_conversion`, on by default, turns such a column into GEOMETRY *when the spatial extension is on the machine* — it is on Hants' Mac, not in the container, which is why the suite never saw it — and the Iceberg scan then has to cast GEOMETRY to the catalog's binary, a cast that does not exist even with spatial loaded. The engine and dbt's connection now `SET enable_geoparquet_conversion = false`: the WKB bytes come through as the catalog says, and `st_geomfromwkb`/`st_astext`/`st_aswkb` are core functions in DuckDB 1.5 for anyone who wants the geometry. `test_a_geoparquet_column_reads_as_the_binary_the_catalog_says` (Moto; a DuckDB-written GeoParquet prefix) pins it; `/docs/remote`'s Overture section says so. The probe also tried the format-version 3 route (an Iceberg `geometry` column): pyiceberg 0.12 refuses to write V3 metadata, recorded against V1 in `TASKS.md`, with the catalog's bare-500 answer as a small open item. With the rows back, the grid showed the geometry as `{"0":1,"1":1,…}` — `JSON.stringify` of a `Uint8Array`; a binary cell is now its size and first eight bytes in hex (`21 bytes · 0101000000000000…`), pinned in `Grid.test.tsx`.

## 13. Open

A retention or a switch for history's per-run rows, if they ever weigh (Hants: "we can modify the rules later") — in `TASKS.md`. Ship stays held. P2 waits for the ship brief to run. All five agreed items of `decisions-for-review_092026.md` are built; the Mac run of P1 needs `tauri dev`, because the folder is the shell's to make.
