{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Every project window: the shell's own commands and Tauri's core events only; the folder dialog is opened from Rust.",
  "windows": ["project-*"],
  "permissions": ["core:default", "core:event:default", "core:window:default"]
}
