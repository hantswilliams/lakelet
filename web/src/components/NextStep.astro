---
// Copyright 2026 Lakelet contributors
// SPDX-License-Identifier: Apache-2.0
import { url } from '../lib/url';
interface Props { title?: string; text?: string }
const { title = 'Your data. Your next question.', text = 'Open source. Available from source on macOS and Linux.' } = Astro.props;
---
<section class="next-step section-frame"><div><p class="eyebrow">Start small. See what fits.</p><h2>{title}</h2><p>{text}</p></div><a class="button" href={url('/docs/install')}>Try Lakelet from source <span aria-hidden="true">↗</span></a></section>
